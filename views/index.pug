doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title 9ja Food Tracker😋 - #{title}
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.7.2/css/all.min.css")
    link(rel="stylesheet", href="/css/style.css")
  body#background
    // partial:index.partial.html
    h2 9JA Food Tracker
    #container.container
      .form-container.sign-up-container
        form#signUpForm
          h1 Create Account
          input#name2(type="text", placeholder="Name" required)
          input#email2(type="email", placeholder="Email" required)
          input#password2(type="password", placeholder="Password" required)
          button Sign Up
      .form-container.sign-in-container
        form#signInForm
          h1 Sign in
          input#email(type="email", placeholder="Email" required)
          input#password(type="password", placeholder="Password" required)
          a(href="#") Forgot your password?
          button Sign In
      .overlay-container
        .overlay
          .overlay-panel.overlay-left
            h1 Welcome Back!
            p Please login with your personal details
            button#signIn.ghost Sign In
          .overlay-panel.overlay-right
            h1 Hello, Buddy 🙎‍♂️!
            p Enter your personal details and Check your diets
            button#signUp.ghost Sign Up
    footer
      p
        | Created with 
        i.fa.fa-heart
        |  by 
        a(target="_blank", href="https://github.com/GodblessMe2") Alptem Technologies
        p &copy; Copyright 2023.
    // partial
    script(src="/js/main.js")
    script(src="/js/bundle.js")

